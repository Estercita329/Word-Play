$(document).ready(function() {
  $("form").submit(function(event) {
    event.preventDefault();
    const sentence = $("input#sentence").val().split(" ");
    let reversedSentence = [];
    
    sentence.forEach(function(word) {
      if (word.length >= 3) {
        
      }
    });





  });
});